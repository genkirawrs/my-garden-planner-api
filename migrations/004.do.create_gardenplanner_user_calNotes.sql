CREATE TABLE gp_user_calNotes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER NOT NULL
	REFERENCES gp_users(id) ON DELETE CASCADE,
    day INTEGER NOT NULL,
    notes TEXT
);
